# 1. write a program that prints out all the elements of the list that are less than 5.

my_list = [1, 7, 2, 3, 5, 8, 13, 2, 1, 34, 55, 89]


for element in my_list:
    if element < 5:
        print(element)

# 2. Instead of printing the elements one by one, make a new list that has all the elements less than 5 from this list
# in it and print out this new list.

new_list = []
for element in my_list:
    if element < 5:
        new_list.append(element)

print(new_list)

# 3. Ask the user for a number and return a list that contains only elements from the original list a that are smaller
# than that number given by the user.

number = int(input("Please enter a number: "))

new_list = []
for element in my_list:
    if element < number:
        new_list.append(element)

print(new_list)