# Write me a python function that inputs a string and a list, then uses the list values to split the string into another list.

a_str = "a1,b1,c1"
a_list = [',']
'''
def splitter(a_str, a_list):

    result=a_str.split(a_list[0])

    print(result)
    return(result)

if __name__ == "__main__":
    splitter(a_str, a_list)
'''

def my_func:
    b = a_str.split(a_list[0])

    print(b)
    return(b)

if __name__ == "__main__":
    my_func()